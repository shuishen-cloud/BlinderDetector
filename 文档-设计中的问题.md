# 设计中的问题 —— 自检记录

> 2026-09-20　｜　自检范围：提交 `245c6b3`（统一帧入口 + Web 调试台 + 拆掉服务端播放状态机）
>
> 这份文档记**已知的问题和取舍**，不是待办清单。每条都标了「是否为真缺陷」，
> 以及要修的话怎么修。**它存在的意义是：别让下一轮开发和答辩时把这些当成「已经做好了」。**

---

## 零、最需要先处理的一条：仲裁能力成了空洞

**「打断 / 积压保护 / 最短播放时长」这三个能力，现在既没有实现，也没有测试。**

- 它们随服务端播放状态机一起被拆掉了（见 [docs/design.md](docs/design.md) D11）；
- 按设计它们归端侧，**而端侧还没写**；
- `min_play` 在整个 `app/` 里已经搜不到；
- 服务端已无任何优先级比较、无「当前播报」状态（已核对）。

拆掉这些**本身不是错的**（那套设计必然产生静默 bug，理由见 D11）。错的顺序是：
**我在动之前没有确认「任务书里仲裁算不算一项交付能力」。**

| 如果 | 那么 |
| :--- | :--- |
| 任务书**要求**播报仲裁 | 这是个**交付缺口**，答辩会被问到。要么在端侧实现（输入已齐：`priority` + `interrupt` + `ttl_ms` 都在契约里，且 `safety.py:114`、`fall.py:240` 仍在设置 `interrupt`），要么把服务端那套恢复回来（`git show 94b7682:app/core/arbiter.py` 是完整原版） |
| 任务书**没要求** | 删得对，无需处理。把 D11 里「此项已不在服务端」写得更显眼即可，免得被误读成「漏做了」 |

**这一条我没法自己判断，需要你查任务书**（已登记在 `工作管理.md` §六 #4）。

---

## 一、过度设计

我（Claude）在这轮加的东西里，以下几处超出了当时的需求。

### 1.1 ★ `CORS_ORIGINS` + `CORSMiddleware` —— 当前用不到

调试台是**同源托管**的（`GET /` 由后端自己发），所以浏览器根本不发跨源请求。
我是为「将来 App 跨源访问」加的，属于典型的 YAGNI。

- **判断**：过度设计。配置项 `CORS_ORIGINS` 加默认值 `*` 是「先加上反正不亏」的思路。
- **要修**：删掉 `CORSMiddleware` + `config.CORS_ORIGINS` + `.env.example` 那一行，
  真需要跨源时再加回来（三行的事）。
- 保留的唯一理由：如果你用 `file://` 直接打开 `web/index.html` 调试（不经后端），
  那就需要 CORS。**但推荐的做法是走后端同源，而不是留一个用不到的白名单。**

### 1.2 `web/index.html` 体量偏大（437 行）

调试台的**最小必要集**只有：发帧 + 看播报。实际做了：TTS 朗读、四种震动模式的
视觉模拟、TTL 过期灰化、WS 自动重连、健康状态轮询、请求日志截断、拖拽上传、
100 条上限……

- **判断**：部分是必要的（拖拽、日志），部分是锦上添花（TTS、震动模拟、过期灰化）。
- 我倾向**保留 TTS**（评估 `phrasing.py` 的措辞真的需要听一遍），但震动模拟和
  过期灰化可以删。
- 顺带：它是**单文件全部内联**，所以 CSS/JS 都挤在一个 HTML 里，可读性已经开始变差。
  再长下去该拆成 `web/index.html` + `web/console.js` + `web/console.css`。

### 1.3 上传路由的校验分支偏多

`POST /v1/frame` 有 7 个 400 分支（缺 `image`、`image` 是文本字段、`source` 非法、
`extra` 非 JSON、`extra` 非对象、`ts` 非整数、空文件）。

- **判断**：**这条我不确定是过度设计。** 严格校验能让前端早暴露错误，
  对一个「用来验证契约」的调试接口是有价值的。但 `ts 非整数也 400` 这种
  确实可以放宽（容错成「忽略、用服务端时间」）。
- 留给你判断，我没动。

### 1.4 `Arbiter` 的 docstring 比代码长

86 行文件里约 25 行是「为什么拆掉旧设计」的说明。

- **判断**：内容有价值，但**放错了地方** —— 这段历史属于 `docs/design.md` D11
  （已经写了一份），代码里留一行指过去就够了。

### 1.5 `abs(now_ms - last)` 防未来时钟

去重比较我写成了 `abs(now_ms - last) < window`，为的是防止一个未来时间戳把
某个 key 永久锁死。

- **判断**：**保留**，但要点名它是个「为假想输入加的防护」。
  理由成立：障碍物的 `dedup_key` 不带事件号（同一种台阶永远同一个 key），
  被锁死就再也播不出来，而 `Frame.ts` 是客户端可控的。
- 这是一行代码解决一个真问题，我认为不算过度设计 —— 但它是**唯一**一处
  我主动为「时钟不可信」写的东西，所以列出来让你知道。

---

## 二、真实缺陷与风险

### 2.1 ★★ 上传的帧落在一个**被静态托管**的目录里 —— 隐私泄露

**已实测：**

```
GET /data/uploads/probe_test.jpg → 200 FAKE-CAMERA-FRAME-SECRET
```

**根因**：我选了 `UPLOAD_DIR = DATA_DIR / "uploads"`，而 `main.py` 里
`Mount("/data", StaticFiles(directory=DATA_DIR))` **把整个 `data/` 目录公开了**。
于是摄像头帧在落盘的那一小段时间内是可以被任何人取走的。

窗口很短（响应返回前就删），但**这是设计错误不是时序问题**：

- 攻击者可以轮询 `/data/uploads/`（前端默认 600ms 一帧，撞上很容易）；
- 更根本的是：**隐私敏感的摄像头画面根本不该出现在公开目录下**。

**要修**：把临时帧挪出 `data/`，例如 `tempfile.mkdtemp()` 到系统临时目录。
或者把静态托管收窄，只挂 `data/frames` 和 `data/demo.mp4`，不挂整个 `data/`。
**两个都做更稳。**

### 2.2 ★★ `sent` / `dropped` 无界增长，且每个响应全量序列化

**已实测**（同一进程内连续请求）：

```
sent 累计   75 条，响应体 2243 字节
sent 累计   76 条，响应体 2387 字节
```

**两个问题叠在一起：**

1. `Arbiter.sent` / `dropped` 是**只增不减**的 list（`arbiter.py:70`、`:85`），
   进程活得越久占得越多；
2. `main.py:152` 的响应信封是 `"sent": sorted(arbiter.sent_ids)` ——
   **把累计全集塞进每一个响应**。

按 1 条/秒估，跑一天到十万级，每个上传响应都要带上十万个 id。既是内存泄漏，
也是响应体膨胀（前端每 600ms 收一次）。

**要修**：`sent` 只留最近 N 条（或改成计数器），响应只回报
`{"sent_count": n, "recent": [...]}`。调试台现在只读 `a.sent?.length`，
改成读计数即可。

### 2.3 ★ 上传没有大小上限

**已实测**：**3 MB 的图照样 200 通过**（`request.form()` 的默认
`max_part_size=1048576` 没有拦住它 —— 文件部分不受这个上限约束）。

于是 `upload.read()` 把任意大的文件全量读进内存，再无限制写到磁盘。

**要修**：在读之前检查 `upload.size` 并设上限（比如 8 MB），超限返回 413。
**`upload.size` 是好用的**（已实测：上传 1,500,000 字节 → `size == 1500000`），
不需要自己数字节。手机一帧几 MB 是正常的，所以上限要给够，但不能没有。

### 2.4 一帧图像被搬运三遍

请求体 → `upload.read()`（内存）→ 临时文件（磁盘）→ `read_image()` 再读回内存。

这套磁盘往返**唯一的目的**是保住 `image_ref` 是「服务端路径」这个语义，
好让 `layers/perception.py` 和将来的真实检测器一行都不用改。

**取舍**：改动最小 vs 一次多余的磁盘往返。局域网原型阶段我认为可以接受，
但要记着：**真实部署（手机 4G 传帧）时这笔开销会变成实打实的延迟**。
更彻底的做法是让第一层同时接受 bytes，但那样 `read_image()` 和检测器接口都要动。

### 2.5 ★ 调试台会「静默停止发帧」

```js
async function grabAndSend(source) {
  if (!video.videoWidth || video.paused) return;   // ← 静默 return，无任何提示
```

视频被暂停、播完、或解码出错时，帧就不再发出去，而**页面上没有任何迹象** ——
看起来和后端坏了一模一样。

**这一条要特别重视**：你已经因为这个现象排查过两轮了（两次都是真的后端 bug，
所以第三轮如果发生，很可能被当成「又坏了」而浪费一次排查）。
**至少要在按钮上显示「正在发帧 / 已停止」和已发帧数。**

### 2.6 调试台不限制并发发送

`setInterval` 不会等上一次 `fetch` 完成。服务端一慢就在客户端堆请求，
而堆起来的那些帧到达时早就过期了（`ttl_ms` 已被消耗完），纯属浪费。

**要修**：加一个 in-flight 标记，上一发没回来就跳过这一拍。

### 2.7 整个 `data/` 目录被公开托管

现在 `data/` 里只有生成的测试素材，所以除了 2.1 没别的问题。但这是个**敞口**：
以后任何人往 `data/` 里放东西（日志、导出的帧、标注数据），都会自动公开。

---

## 三、已知取舍与待决策

### 3.1 D11 的代价：端侧仲裁**现在还不存在**

服务端拆掉播放状态机后，排序 / 打断 / 积压 / 到期不补播归端侧。但：

- **真端（Android / 小程序）还没写**，所以这套仲裁目前没有任何实现；
- **调试台是按到达顺序展示的**，不做仲裁 —— 对调试台这是对的（它就该看到全部），
  但**别把它当成「端侧仲裁已经做好了」**。

「换了载体就要重写一遍端侧仲裁」是这个决策明码标价的代价，已写进 `design.md` D11。

### 3.2 交付载体未定（`工作管理.md` §六 #2）

它直接决定 3.1 的端侧仲裁要实现几遍，以及第四层能拿到哪些能力（侧键、后台跌倒）。
**挂了两轮了，越早定越省事。**

### 3.3 `Arbiter` 这个名字现在名不副实

它不再仲裁顺序，只做「发不发」的闸门。改名（如 `AnnouncementGate`）会牵动
`arbiter.py` / `main.py` / `tests/` / `docs/design.md` / `README` / `工作管理`，
我判断不值得为命名做这次churn，**留给你定**。

### 3.4 `测试结果.md` —— 已刷新（2026-09-20）

它原本写的是「140 项测试」（对应提交 `55641f9`），并逐条列了 `test_arbiter.py`
里**已经被删除的用例**。已按提交 `245c6b3` 全面刷新：157 项、用例清单重列、
端到端数字重测、并在开头加了一句「测试若有改动请重跑并更新本文档」。

> **这类文档会持续过期。** 它现在是准的，但下次改测试后又会不准 ——
> 如果能接受，考虑把它改成脚本生成（像 `export_contract.py` 那样）。

### 3.5 README「环境说明」—— 已修正（2026-09-20）

原文写「当前开发环境是 Termux / Android（Python 3.14）」，已改为
「**WSL / Linux（Python 3.13）**；早期开发在 Termux / Android 上做，手机端仍是
目标运行环境之一 —— 零编译约束来自它」。

> 保留了 Termux 的说明而不是删掉：那条约束（不用 FastAPI、不带 `[standard]`）
> 是从它推导出来的，删了理由就断了。**如果手机端最终不跑 Termux，D10 和这段
> 都该重写**，但那是另一个决定。

### 3.6 `smoke.sh` 的端口被占时不会快速失败

它硬等 40 轮再让 curl 报错，错误信息是 `Expecting value: line 1 column 1`，
看不出真正原因是端口冲突。加一次 bind 预检即可。（这条是上上轮就发现的，一直没动。）

# 新功能与接口改动

> 2026-09-20　｜　范围：**接真实外部服务** + **先修已知缺陷**（本轮定下的两个方向）
>
> 这份文档回答一个问题：**要做这些事，得改哪些接口？**
> 所有「缺口」都标了实测证据，不是推测。缺陷编号沿用
> [文档-设计中的问题.md](文档-设计中的问题.md)。

---

## 零、结论速览

**绝大多数功能不用动 `Frame` / `Announcement`。真正需要动契约的只有 3 处**（下表 ★）。

| 事项 | 动 `Frame`? | 动 `Announcement`? | 新路由 | 新注册类别 | 依赖风险 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1.1 真实云端 VLM | 否 | 否 | — | — | 无（有现成实现） |
| 1.2 真实检测模型 | **可能**（若端侧算） | 否 | **可能** `/v1/safety/grade` | — | ★★ **与零编译约束冲突** |
| 1.3 真实地图 API | 否 | 否 | — | `router` | 无 |
| 1.4 真实短信/推送 | 否 | ★ **可能要加投递状态** | `GET /v1/emergency/events/{id}` | `notifier` | 无 |
| 2.1 上传目录隐私泄露 | 否 | 否 | — | — | 无 |
| 2.2 响应信封全量 `sent` | 否 | 否 | — | — | 无（但**前端要跟着改**）★ |
| 2.3 上传无大小上限 | 否 | 否 | — | — | 无（新增 413 语义）|
| 2.4 一帧搬三遍 | 否 | 否 | — | — | 无（可选重构） |
| 2.5 / 2.6 调试台两个问题 | 否 | 否 | — | — | 纯前端 |

★ 三处需要动契约/接口：**① 端侧检测若走端侧需要新入口**、**② 应急投递状态**、**③ 响应信封的 `sent` 字段**。

> **`Frame` / `Announcement` 本身至今没有任何破坏性改动**，这一轮也不需要动它们
> —— 这是「只有两个数据结构」这个设计撑住了的地方。

---

## 一、接真实外部服务

### 1.1 真实云端 VLM —— **不是「只填 `.env`」**

**现状**：`app/core/providers/openai_compat.py` 已实现 DashScope / 智谱 / OpenAI
三个子类（都走 OpenAI 兼容协议），换厂商只改 `.env`。这部分是**现成的**。

**但实测发现三个缺口，不修就用不了：**

**① prompt 只要一句话，导致去重粒度塌成 1 个 key（最严重）**

prompt 写的是「只输出那句话，不要解释」，返回的 detail 是
`vision_detail(text, scene_conf=0.8)` —— `objects` / `ocr_results` **永远是空列表**。
而场景分类（`rules/scene.py`）是靠 `objects` / `ocr` 推导 `scene_key` 的。实测：

```
前方有个红绿灯，现在是红灯  → scene_key=default → dedup=vision:scene:default
左侧一米左右有人经过        → scene_key=default → dedup=vision:scene:default
前方人行道畅通             → scene_key=default → dedup=vision:scene:default
```

**三种完全不同的场景，去重键一模一样。** 后果：第一层会反复播同一句，
**场景变了也不播**（在闸门看来全是「同一个 scene 的重复」）。
也就是说，现在把 `VLM_PROVIDER` 一改，第一层等于废掉。

> **要修**：改 prompt 要求返回 JSON（`scene_description` + `objects[]` + `ocr_results[]`
> + `scene_conf`），并写解析与容错。**契约不用动** —— `vision_detail()` 的形状就是为此设计的。

**② `scene_conf` 硬编码 0.8**

真实 VLM 的置信度没被提取出来，于是 `phrasing.hedge()`（低置信度对冲措辞）
**永远不会触发**。随 ① 一起修。

**③ 网络错误会变成 500，而且没有任何降级通告**

`describe_frame()` 里没有 `try/except`，`r.raise_for_status()` 一抛就冒到路由 →
客户端拿到 **500**，且**一条播报都不发**。

更麻烦的是：**`main.py:304` 定义了 `broadcast_degraded()`，但全项目没有任何地方调用它**
（只有 `app.state.broadcast_degraded = broadcast_degraded` 这行赋值）。
所以 design.md 反复强调的「沉默不能有歧义」**在运行时失效这条路径上没有实现**。

> **要修**：给 provider 加异常兜底（超时/401/429 → 返回空 + 记降级），
> 并把 `broadcast_degraded` 真正接上（路由捕获到层异常时调用）。
> 这是「接真实服务」的**前置条件** —— 真实服务一定会失败。

### 1.2 真实障碍物检测模型 —— 有个架构级冲突

**现状**：`detect(frame) -> list[dict]`，返回**未分级、未措辞**的原始结果，
分级/措辞在 `rules/risk.py` + `phrasing.py`。这个分工很好，换模型不动安全策略。

**冲突：真实检测库几乎都是 C 扩展，与项目的零编译约束冲突。** 实测 wheel 标签：

| 包 | wheel | 能否在 Termux 上装 |
| :--- | :--- | :--- |
| `onnxruntime` | `cp313-cp313-manylinux_2_28_x86_64` | ❌ C 扩展，android/aarch64 无 wheel |
| `opencv-python-headless` | `cp37-abi3-manylinux_2_28_x86_64` | ❌ 同上 |
| `ultralytics` | `py3-none-any` | ⚠️ 本身纯 Python，但**依赖 torch**（巨大且是 C 扩展） |

> `requirements.txt` 顶部写着「所有包均为纯 Python wheel，Termux 上零编译安装」。
> **引入任何真实检测库都会打破这条约束。**

**两条路，必须选一条：**

| | A. 服务端跑检测 | B. 端侧跑检测（design.md D2 已预告） |
| :--- | :--- | :--- |
| 做法 | 服务器装 onnxruntime/opencv | 手机上跑 ONNX，把算出的障碍物上报 |
| 代价 | **Termux 不能再当服务器**（得跑在正常 Linux 上），依赖树变重 | **需要一个新入口**把障碍物送进服务端做分级措辞 |
| 契约影响 | 无 | ★ 需要新路由（见表下） |

**若走 B**，服务端必须仍负责分级/措辞（那是安全关键逻辑，不能散到端上），
所以需要一个新接口，例如：

```
POST /v1/safety/grade
{ "frame": {...}, "obstacles": [ {type, position, distance_m,
                                 distance_sigma_m, confidence, ...} ] }
→ Announcement（走同一套 risk.py + phrasing.py）
```

三个选择：**新路由**（最清晰）／塞进 `Frame.extra`（省事但把 `extra` 当成了数据通道，
它本来是「上下文」）／端侧自己分级（**不要**，安全策略会分叉）。

**另一个小问题**：`read_image()` 在 `app/core/layers/perception.py:32` ——
**在 layer 模块里**。检测器要读图得反过来 import 一个 layer，层次是歪的。
建议挪到 `contracts.py` 附近或单独的 `app/core/images.py`。

### 1.3 真实地图 API

**现状**：`rules/route.py` 用内置假路网（`_FAKE_NETWORK`、`plan(req) -> dict`、
`RouteRequest`、`AVOID_LABELS`、`DEFAULT_AVOID`）。产出 `route_detail()` 形状 —— 这部分是好的。

**改动**：
- `plan()` 现在是**同步**函数，换成 HTTP 调用要变 `async`，`layers/navigation.py` 的调用点跟着改；
- 需要一个新的注册类别 `router`（例：`@register("router", "amap")`）。**注意**：
  `registry.load_all()` 只扫描固定的四个包（`providers` / `detectors` / `layers` / `sources`），
  所以新文件必须放进其中之一（放 `providers/` 最省事），否则不会被自动加载。
- 契约**不用动**：`route_detail` 形状不变，`extra.destination` / `extra.geo` / `extra.avoid` 已够用。

> **顺手修掉一个文档 bug**：`extra.geo` 的格式在生成文档里原本是
> `('lat', 'lng')` —— 一个 **Python 元组**，因为 `export_contract.py` 的 f-string 里
> `{"lat", "lng"}` 未转义大括号、被当成表达式求值了。前端照这个写必然对不上。
> **已修**，现在输出 `{"lat": 39.9, "lng": 116.4}`。

### 1.4 真实短信/推送通道 ★ 最要紧的一条

**先说一个必须正视的问题：现在的「已通知您的家人」是空话。**

```
app/core/rules/sos.py:42-43    "family": "已通知您的家人"
app/core/rules/fall.py:268     "已通知您的家人，请保持冷静，不要移动。"
```

全项目 `rules/` 与 `layers/` 里**没有任何发送实现**（无 SMTP、无 webhook、无推送 SDK）。
播报文本却直接断言「已通知」。对普通应用这只是没做完；**对视障安全系统，
这是在用户最危险的时刻告诉他一件没发生的事**——他会因此停止自救、原地等一个不会来的人。

> **这不是「接个通道」的事，是诚实性问题。** 建议两步走：
> 1. **立刻**把措辞改成不含断言的说法（如「正在通知您的家人」），直到真有通道；
> 2. 接入通道后，把**投递结果**如实反映到文本上（成功才说「已通知」）。

**接口改动**（接通道时）：

- 新增注册类别 `notifier`：`async def send(scope, text, location) -> DeliveryResult`；
  实现放 `app/core/providers/`（同 1.3 的扫描约束）。
- ★ **`Announcement` 可能要加字段**：投递失败时 `text` 必须不同，
  `detail.emergency` 需要能表达 `delivery: {channel, ok, error}`。
  好在这属于**只加可选字段**，符合兼容性原则。
- **家属远程查看**（`工作管理.md` §一 分工表里第四层已列）需要新路由，
  例如 `GET /v1/emergency/events/{id}` 返回事件状态 + 最后位置。
  这是个**只读查询接口**，与现有「入 Frame 出 Announcement」的九条路由形状不同
  —— 是这套契约第一次出现「非 Frame 入参」的路由，需要先想清楚它的响应形状。

---

## 二、先修已知缺陷（逐条给接口影响）

| # | 缺陷 | 动契约? | 要做的事 | 风险 |
| :--- | :--- | :--- | :--- | :--- |
| 2.1 | 上传帧落在被静态托管的目录（**隐私泄露**，已实测 200 取回） | 否 | `UPLOAD_DIR` 移出 `data/`（用 `tempfile.mkdtemp()`）；并把 `Mount("/data")` 收窄到只挂 `data/frames` + `demo.mp4` | **两个都做**才稳 |
| 2.2 | `sent`/`dropped` 无界增长 + 每响应全量序列化 | ★ 信封 | 保留最近 N 条；信封从 `"sent": [...全部 id]` 改为 `{"sent_count": n, "recent": [...]}`。**前端 `a.sent?.length` 要跟着改** | 不是 `contracts.py` 的一部分，但前端依赖它 |
| 2.3 | 上传无大小上限（实测 3 MB 通过） | 否 | 用 `upload.size`（**实测可用**）判上限，超限返回 **413** | 新增一个状态码语义 |
| 2.4 | 一帧图像被搬运三遍（内存→磁盘→内存） | 否 | 可选：让第一层同时接受 bytes；或保留现状（局域网原型可接受） | 要动 `read_image()` 与检测器接口 |
| 2.5 | 调试台静默停止发帧（`video.paused` 直接 return） | 否 | 纯前端：按钮显示「正在发帧/已停止」+ 已发帧数 | **优先做**，你已经因此误判过两轮 |
| 2.6 | 调试台不限制并发发送 | 否 | 纯前端：加 in-flight 标记 | 低 |
| 2.7 | 整个 `data/` 被公开托管 | 否 | 与 2.1 同一次修 | 敞口 |

---

## 三、契约改动汇总（要动的地方只有这些）

| 改动 | 类型 | 兼容性 | 谁受影响 |
| :--- | :--- | :--- | :--- |
| `detail.emergency` 加 `delivery` 字段 | **只加可选字段** | ✅ 符合兼容性原则 | 第四层、前端 |
| 新增路由 `/v1/frame`（已完成） | 新增 | ✅ 已加可选路由 | 前端 |
| 新增路由 `POST /v1/safety/grade`（若端侧检测） | 新增 | ✅ | 第二层、端侧 |
| 新增路由 `GET /v1/emergency/events/{id}` | 新增（**首个非 Frame 入参**） | ✅ 但形状需先定 | 第四层、家属端 |
| 响应信封 `arbiter.sent` 改计数 | 观测字段改名/改形 | ⚠️ **前端要跟** | 调试台 |
| 新增注册类别 `router` / `notifier` | 扩展 | ✅ | 第三、四层 |
| `Frame` / `Announcement` **顶层字段** | **不动** | ✅ | — |

> 按 `工作管理.md` §三 的流程：上面每一条都要先登记到 §五 变更记录，
> 改 `app/contracts.py` 后跑 `pytest tests/test_contracts.py`，再通知受影响的人。

---

## 四、建议顺序

1. **先修真缺陷**，尤其 **2.1 隐私泄露**（现在就在漏）和 **2.5 调试台静默停发**
   （它会让你把真 bug 误判成「又坏了」）。
2. **「已通知」的诚实化 + 把 `broadcast_degraded` 接上** —— 这两件是一类事：
   系统不能在没做到的时候说做到了。而且它们**必须早于接真实服务**，
   因为真实服务一定会失败。
3. **2.2 信封膨胀** —— 顺手改，前端同步。
4. **真实 VLM** —— 见效最快（现成实现，只差 prompt 与降级），且能立刻暴露 1.1① 那个去重塌缩。
5. **真实地图 API** —— 独立，不阻塞别人。
6. **检测模型** —— 先定「服务端还是端侧」这个架构问题，再动手。
7. **端侧仲裁**（D11 的代价）—— 最重，且要先定交付载体（`工作管理.md` §六 #2）。

> 第 7 项与 `文档-设计中的问题.md` §零 是同一件事：**若任务书要求播报仲裁，
> 它现在是缺口**。那件事的优先级可能高于本表所有条目 —— 建议先确认。

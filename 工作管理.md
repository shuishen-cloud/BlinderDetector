# 灵眸伴途 —— 工作管理

> 更新日期：2026-09-20　｜　当前阶段：**接口契约冻结，四层逻辑已实现，各组并行开工**

**文档索引**

| 想知道 | 去哪看 |
| :--- | :--- |
| 怎么跑起来 | [README.md](README.md) |
| 接口字段、路由、枚举 | [docs/api-contract.md](docs/api-contract.md)（自动生成，勿手改） |
| **为什么这么设计** | [docs/design.md](docs/design.md) |
| 谁做什么、进度、规范 | 本文档 |
| 设计中的已知问题与取舍 | [文档-设计中的问题.md](文档-设计中的问题.md) |
| **新功能与接口改动计划** | [新功能与接口改动.md](新功能与接口改动.md) |
| **待处理的遗留问题（可排期）** | [问题-待处理的一些遗留问题.md](问题-待处理的一些遗留问题.md) |

---

## 一、项目分工

| 层 | 功能 | 负责人 | 状态 |
| :--- | :--- | :--- | :--- |
| 第一层 环境感知 | 实时场景描述、OCR 播报、物体识别 | 组长 | 契约已定，待实现 |
| 第二层 安全预警 | 障碍物检测、深度估计、跌倒检测 | 组长 | 契约已定，待实现 |
| 第三层 智能导航 | 无障碍路线规划、语音交互、实时纠偏 | 组员 | 契约已定，待实现 |
| 第四层 紧急求助 | 一键求助、家属远程查看、社区联动 | 组员 | 契约已定，待实现 |
| 契约与骨架 | `Frame` / `Announcement` / 播报闸门 / 注册表 | 组长 | ✅ 已完成 |

---

## 二、任务看板

### 待办

- [ ] 第一层：接真实云端 VLM（`VLM_PROVIDER=dashscope` 等），需 API key
- [ ] 第二层：接真实障碍物检测模型（`detectors/` 下加实现）
- [ ] 第二层：接真实深度估计，产出可信的 `distance_sigma_m`
- [x] ★ 第三层：**百度地图步行路线规划接入并真调打通**（2026-09-21）——
      新增 `app/core/routers/` 注册类别（`builtin` / `baidu`），
      `ROUTER` 走 `.env` 切换，失败自动降级回内置路网并**如实播报**。
      服务端 AK 已配好，实测太原工业学院→太原站 12.55 公里 / 29 段，
      扫出 2 条真实无障碍警告（台阶、地下通道）。
      **仍待办**：① 地理编码（让「最近的地铁站」这种地名能直接用，
      需再接百度地点检索，本轮明确跳过）；② 端侧 GPS 定位 ——
      调试台已接浏览器 `geolocation`（2026-09-23，进导航卡自动取起点，
      失败如实降级并播报；**只在 https / localhost 下可用**），
      真机端侧（App / 小程序）的 GPS 仍待办
- [ ] 第三层：高德 API 接入（作为一个新的 `@register("router", "amap")` 实现）
- [x] ★ 第四层：**「已通知您的家人」曾经是空话** —— 全项目无发送实现。
      两步走：**第一步（措辞去断言）已完成** —— `sos.py` / `fall.py` 的文案
      全部改成「正在通知…」，`tests/test_fall.py`、`tests/test_sos.py`
      各加一条回归用例钉住这个不变式。
      第二步（接入通道后如实反映投递结果、加 `detail.delivery`）仍待办。
      详见 [问题-待处理的一些遗留问题.md](问题-待处理的一些遗留问题.md) §1.5
- [ ] 第四层：接真实短信/推送通道（新增注册类别 `notifier`）
- [ ] 后端：把 `broadcast_degraded` 真正接上（已定义但**全项目无人调用**），
      让「沉默不能有歧义」在运行时失效这条路径上生效
- [x] ★ 前端：**路线可视化面板**（2026-09-21）—— `web/map.js` 把后端返回的
      路线几何画到百度 JSAPI GL 底图上（起终点标记取自几何本身；一条黄色
      虚线标出「输入框的 WGS-84 起点」与「路线的 BD-09 起点」之间的偏差）。
      **断网或没配 `BAIDU_BROWSER_AK` 时退回纯 SVG 无底图示意图** ——
      失败只落在卡片里，不影响播报流与请求日志
- [ ] 前端：消费 WebSocket 播报 + TTS + 震动
- [ ] 前端：把调试台的摄像头占位（`web/index.html` 末尾 `startCamera`）换成
      真的 `getUserMedia` 取流 —— 复用同一个 `sendFrame()`，后端不用改

### 进行中

- （无）

### 已完成

**2026-09-20 骨架与契约**
- [x] 环境验证：确认 Termux 无法使用 pydantic，改用 Starlette + 标准库 dataclass
- [x] 接口契约冻结（`app/contracts.py`，只有 `Frame` + `Announcement` 两个结构）
- [x] 播报出口（`app/core/arbiter.py`）
- [x] 实现注册表（`app/core/registry.py`），组员加实现不改别人代码
- [x] 输入源：视频 / 图片序列两个实现，产出同样的 `Frame`
- [x] 九条路由 + WebSocket 推流（`app/main.py`）
- [x] 测试视频生成链路（手写 SVG → rsvg-convert → ffmpeg）
- [x] 契约文档自动生成（`scripts/export_contract.py` → `docs/api-contract.md`）

**2026-09-20 Lwy 分支 —— 各层逻辑实现（不接外部服务）**
- [x] 第一层：场景分类（决定去重粒度）、置信度对冲措辞、降级路径
- [x] 第二层：风险分级（悲观距离 + 置信度门限）、措辞生成、
      短句降级（近距离预警说不完完整句子）、TTL 随步行速度调整
- [x] 第二层：跌倒状态机完整实现 —— 二次确认、显式取消、宽限期、
      步行节律硬否决、通知链
- [x] 第三层：无障碍路线过滤 + 步骤播报（允许播数字）
- [x] 第四层：幂等、升级链、绝不自动拨 120
- [x] 140 项 pytest 全绿，规则层可脱离框架单独测试
- [x] `scripts/demo_fall.py` 跌倒全流程演示

> 四层的**逻辑**已完整，但外部依赖仍是 mock：不调云端 VLM、不跑检测模型、
> 不连地图 API、不发真实短信。接口已定死，组员可以并行开工。

**2026-09-20 环境迁移验证（Termux → WSL）**
- [x] 新机器按 README 从零复现全链路：140 项 pytest 全绿、smoke 八条路由、
      视频/图片素材生成、跌倒演示、WebSocket 推流
- [x] 修复 `run_video.py --source images` 崩溃（见 §七 缺陷 1）
- [x] 修复真实服务下 `/v1/stream` 404（见 §七 缺陷 2），`requirements.txt` 补 `wsproto`

**2026-09-20 前端准备 —— 统一帧入口 + Web 调试台**
- [x] 摸清契约断点：`Frame.image_ref` 是「服务端路径」，端侧没有传像素的路
      （`layers/perception.py:34` 用 `os.path.isfile` 找文件）；Mock 忽略图像
      字节，把这个缺口掩盖了
- [x] 新增 `POST /v1/frame`（multipart 统一帧入口）—— 摄像头 / 视频抽帧 /
      图片文件共用，信封与其余路由逐字一致
- [x] `requirements.txt` 补 `python-multipart`（`request.form()` 的硬依赖，
      同 wsproto 一样属于「TestClient 测不出」的坑）
- [x] 新增 `CORS_ORIGINS` 配置 + `CORSMiddleware`
- [x] `web/index.html` 调试台：视频抽帧（两条流水线各自独立发包）、
      单图上传、WS 播报流、TTS、震动模拟、其余路由按钮、健康状态
- [x] 静态托管：`GET /` 出调试台，`GET /data/*` 出测试素材
- [x] 158 项 pytest 全绿（新增 18 项覆盖上传路由与静态托管）

> 调试台不是交付形态，只用来**验证契约**：让「帧 → 后端 → 播报 → WS 推流」
> 这条真实链路能被肉眼看见。摄像头的取流留了接口占位（见待办）。

---

## 三、接口契约约定

**`app/contracts.py` 是唯一真源。** 全系统只有两个数据结构：

```python
Frame         # 所有接口的输入
Announcement  # 所有接口的输出
```

十条路由全部「入 `Frame`，出 `Announcement`」，层的差异只体现在 `source` 字段和 `detail` 的形状。
其中 `POST /v1/frame` 是 multipart 统一帧入口（端侧传图像用）。

### 改契约的流程

契约是四层并行的前提，**改动必须走流程，不能各自改**：

1. 在本文档 §五 变更记录里登记（日期、改什么、为什么、影响哪几层）
2. 同步修改 `app/contracts.py` 和 `app/mock/fixtures.py`
3. `pytest tests/test_contracts.py` 必须全绿
4. 通知所有受影响的人

**兼容性原则：** 只加可选字段，不改已有字段名和类型，不删字段。破坏性改动要全员确认。

### `detail` 各层的形状

| `source` | `detail.kind` | 关键字段 |
| :--- | :--- | :--- |
| `perception` | `vision` | `scene_description`, `ocr_results[]`, `objects[]` |
| `safety` | `safety` | `risk`, `obstacles[]` |
| `navigation` | `route` | `route_id`, `steps[]`, `total_distance_m`，可选 `geometry[]` / `coord_system`（★ 只用于可视化，已抽稀，**不得据此做任何决策**） |
| `emergency` | `emergency` | `state`, `event_id`, `confirm_deadline_ts` |
| `system` | — | 降级通告 |

完整字段见 `app/contracts.py` 的注释和 `app/mock/fixtures.py` 的样例。

---

## 四、开发规范

### 分支

远程仓库：`git@github.com:shuishen-cloud/BlinderDetector.git`

| 分支 | 用途 |
| :--- | :--- |
| `main` | 只接受能跑通的代码，`pytest` 必须全绿 |
| `Lwy` | 组长分支：骨架 + 契约 + 第一、二层实现 |
| `<姓名>` 或 `feat/层名-功能` | 组员各自的开发分支 |

**流程：**

```bash
git checkout main && git pull          # 从最新的 main 开分支
git checkout -b <你的分支名>
# ... 开发 ...
pytest -q                              # 必须全绿再提交
git add -A && git commit -m "<层名>: <做了什么>"
git push -u origin <你的分支名>
```

然后在 GitHub 上开 Pull Request 合回 `main`。**不要直接往 `main` 推。**

> 网络提示：22 端口在当前网络下被代理拦截，`~/.ssh/config` 里已经把
> github.com 的流量转到 `ssh.github.com:443`。换了新机器要重新配。

一次提交只做一件事。

### 提交信息

```
<层名>: <做了什么>

例如：
safety: 接入 YOLOv8 障碍物检测
contracts: 给 Obstacle 加 track_id 字段
```

### 新增实现的方式

**不要改别人的代码。** 每层都通过注册表接入：

```python
# app/core/providers/zhipu_vlm.py
from app.core.registry import register

@register("vlm", "zhipu")
class ZhipuVLM:
    async def describe_frame(self, frame: Frame, image: bytes) -> dict:
        ...   # 返回 contracts.vision_detail() 的形状

    async def health(self) -> bool:
        return True
```

然后在 `.env` 里改 `VLM_PROVIDER=zhipu` 即可切换。

**检测器只回答「看到什么」**，置信度过滤、风险分级、措辞全在
`app/core/rules/` 里，所有检测器共用同一套安全策略 —— 换模型不会让
安全规则跟着变。详见 [docs/design.md](docs/design.md) D9。

目录约定：

```
app/core/providers/   # VLM 实现
app/core/layers/      # 四层实现
app/core/sources/     # FrameSource 实现（video / images）
```

### 测试要求

- 新增实现要带 pytest 用例
- 改动 `app/contracts.py` 必须跑 `pytest tests/test_contracts.py`
- 涉及仲裁逻辑的改动必须跑 `pytest tests/test_arbiter.py`

### 运行

```bash
pip install -r requirements.txt
cp .env.example .env
bash scripts/smoke.sh                        # 一键自检
python scripts/run_video.py data/demo.mp4    # 喂视频看播报
```

---

## 五、变更记录

| 日期 | 变更 | 原因 | 影响 |
| :--- | :--- | :--- | :--- |
| 2026-09-20 | 契约冻结：`Frame` + `Announcement` | 任务书原数据结构残缺且四层各不相同，无法并行 | 全部四层 |
| 2026-09-20 | 弃用 FastAPI，改 Starlette | Termux 装不了 pydantic-core | 后端 |
| 2026-09-20 | 新增触发方式常量 `TRIGGERS`、求助范围 `SCOPE_*` | 「长按侧键」在小程序/Web 无 API，触发方式改为可协商集合 | 第四层 |
| 2026-09-20 | 新增路由 `/v1/emergency/cancel`、`/v1/emergency/tick` | 取消与状态机推进需要入口 | 第四层 |
| 2026-09-20 | 业务规则从 layer 抽到 `rules/` | 换检测模型时安全策略不应跟着变；规则层要能脱离框架测试 | 后端 |
| 2026-09-20 | `requirements.txt` 新增 `wsproto>=1.2` | 不带 `[standard]` 的 uvicorn 没有任何 WS 实现，`/v1/stream` 直接 404 | 后端 / 前端联调 / 第四层推送 |
| 2026-09-20 | 新增路由 `POST /v1/frame`（multipart 统一帧入口） | 端侧（摄像头 / 视频帧）没有传像素的路径，`image_ref` 只能是服务端已有路径 | 第一、二层 + 前端 |
| 2026-09-20 | `requirements.txt` 新增 `python-multipart`；新增 `CORS_ORIGINS` 配置 | 前者是 `request.form()` 解析 multipart 的硬依赖；后者供调试台与将来的 App 跨源访问 | 后端 + 前端 |
| 2026-09-21 | 新增注册类别 `router` + `app/core/routers/` 包（`builtin` / `baidu`）；`rules/route.py::plan()` 拆成 `routers/`（async 数据源）+ `build_detail()`（同步策略） | 接百度地图步行路线规划；顺带把「假路网混在 rules/ 里」这个分层问题修掉 | 第三层、后端 |
| 2026-09-21 | `extra` 新增**可选**字段 `destination_geo` | ★ 原判断「契约不用动」是错的：`extra.destination` 是自然语言，而地图 API 的 `destination` 要坐标 | 第三层、端侧 |
| 2026-09-21 | 无障碍措辞：真实地图路径**只说「前方有 X，请注意」，不再声称「已避开」** | 百度没有避开天桥的参数，声称「已避开」是假话 —— 与「已通知您的家人」同类 | 第三层 |
| 2026-09-21 | `route_id` 从 `hash()` 改成 `blake2s` 确定性摘要（含 router 名与起点）；新增 `contracts.route_dedup_key()` | `hash()` 每进程随机化，重启后 id 变；且两个字段写死了错误的键格式 | 第三层 |
| 2026-09-21 | `route_detail` 新增**可选**字段 `geometry`（路线折线，已 Douglas-Peucker 抽稀 + 量化到 5 位小数）与 `coord_system`；新增 `GET /v1/frontend-config` 下发浏览器端 AK；`web/map.js` 路线可视化面板 | 让调试台能把路线画出来。实测 537 点 → 61 点、12 KB → 1.2 KB，几何只挂在第一条播报上 | 第三层、前端、契约文档 |
| 2026-09-21 | **百度路线规划真调打通**（太原工业学院→太原站，12.55 km / 29 段），修掉 5 处「文档与实际不符」：字段名是单数 `instruction`、instruction 带 HTML 标签、`turn_type` 可能为空、距离单位有「公里」、台阶措辞是「过阶梯」 | 这 5 条照文档写都发现不了，其中字段名那条会让**整条路线静默解析失败**。详见 `新功能与接口改动.md` §1.3.1 | 第三层、后端 |
| 2026-09-20 | **拆掉服务端播放状态机**：`Arbiter` 从「当前播报 + 优先级队列 + 打断 + 积压保护」简化为只做去重 + 废数据过滤；删除 `_current` / 队列 / `finish_current` / `advance` / `max_queue` / `min_play_ms`。响应信封 `arbiter.spoken` 改名 `arbiter.sent` | 服务端观察不到端侧播放状态，模拟它是两轮「播报流永久静默」bug 的共同根因（详见 design.md D11） | 后端 + 前端（端侧要自己实现排序/打断） |
| 2026-09-23 | 新增 `web/js/speech.js`：**端侧终于把 D11 划过来的活做了** —— p3/`interrupt` 抢占、积压超过 1 条就整批砍掉（`speechStats().dropped` 记账）、语速四档、开机探测语音能力（没有中文嗓音就写「声音：不可用」而不是假绿） | 原来前端只是无条件 `speechSynthesis.speak()`，`interrupt` 字段只被当文本写进 meta 标签。中文 TTS 约 250ms/字，队列单调增长会让用户听到十几秒前的**位置** —— 听见错的比没听见更坏 | 前端（端侧） |
| 2026-09-23 | 断网 / 降级**出声**：`main.js` 断连 5 秒迟滞后播报「网络断开正在重连」，恢复时报出断了多少秒；`dev.js` 健康检查只在「变坏 / 变好」的边沿出声。生死按钮按下即震动 + 播报 | 这两件事原来只改颜色和文字。对看不见屏幕的人，「断开」和「一切正常、只是没人说话」是同一件事 —— 而 README 自己写着「用户会把『没出声』理解成『环境安全』」 | 前端（产品界面） |
| 2026-09-23 | 新增 `web/js/voice.js`：目的地改成**按住说话**（长按起录、松开出结果），识别文本落进 `#dest` 后按下既有的导航按钮 | 目的地是这一页唯一要打字的地方，而用户看不见那格 —— 打字 = 屏幕键盘 + 盲目逐字确认。**不新增请求路径**：语音只换「怎么把字填进去」 | 前端（产品界面 / 端侧） |
| 2026-09-23 | 新增 `web/js/nav.js`（**第三层唯一的提交路径**）：`submitRoute()` 同时供「按住说话」与「打字回车」调用；导航卡**删掉「开始导航」按钮**；起点改成进页面**自动取当前位置**，拿不到就如实降级（起点行标黄 + 播报「这次按默认坐标规划」）；「用当前位置」改成重试用的「重新定位」 | ① 那个按钮对用户完全多余（语音松开即走、打字敲回车），而它当时是这条路上**不可删的一环**（语音靠 `button[data-dest].click()` 去按它）—— 按钮一删，两条输入就只剩一个出口。② 起点原本写死一组坐标 + 一个「用当前位置」，把「他此刻在哪」这个**确定的事实**变成了要用户先意识到、再去按的选项；这一页服务的正是**正在走的人** | 前端（产品界面 / 端侧） |
| 2026-09-23 | `confirmAction` 从 `dev.js` 搬到 `ui.js`（`export`） | 导航（产品功能）也要它，而产品功能不该反向 import 调试件 —— `dev.js` 的头注释正是按这条界线写的 | 前端 |

> **契约兼容性原则：** 只加可选字段，不改已有字段名和类型，不删字段。
>
> `Frame` / `Announcement` 本身**至今没有破坏性改动**。但最后一条
> （拆掉服务端播放状态机）改了 HTTP 响应信封里 `arbiter.spoken` 的字段名
> → `arbiter.sent`。它不是 `contracts.py` 的一部分（只是给调试台看的观测
> 字段），但**读这个字段的前端要跟着改** —— 调试台已同步。

---

## 六、待确认事项

| # | 事项 | 影响 | 状态 |
| :--- | :--- | :--- | :--- |
| 1 | 障碍物距离的播报措辞：用「就在脚前/很近/几米外」粗档，还是「2.0 米」精确值 | 第二层播报文案 | 待组长确认 |
| 2 | 最终交付载体（Android 原生 / 小程序 / Web） | 决定第四层能否做侧键求助和后台跌倒检测 | 待确认 |
| 3 | 是否将人脸识别纳入范围 | 涉及隐私合规，本期已决定不做 | 已决定：不做 |
| 4 | **任务书里「播报仲裁」（打断 / 积压保护 / 最短播放时长）是否算交付能力** | 若是，则该能力目前**既未实现也无测试** —— 已随服务端状态机一起拆掉，需在端侧实现或恢复 | **待确认（较急）** |

---

## 七、环境部署缺陷记录

2026-09-20 在另一台机器（WSL / Linux，Python 3.13）按 README 从零复现全链路时暴露。

> **★ 这两个缺陷都不是 WSL 特有的 —— Termux 上同样会复现。**
> 根因都在依赖和脚本本身，与平台无关，别当成平台差异放过。

### 缺陷 1：`run_video.py --source images` 直接崩

| | |
| :--- | :--- |
| **现象** | `python scripts/run_video.py data/frames --source images` 抛 `TypeError: ImageSequenceSource.__init__() got an unexpected keyword argument 'fps'` |
| **根因** | `scripts/run_video.py` 无条件给输入源传 `fps=`，但只有 `VideoFileSource` 接受该参数；`ImageSequenceSource` 只认 `path` / `source` |
| **修复** | 按输入源类型分别构造 —— 只有 `kind == "video"` 时才传 `fps` |
| **影响** | 仅脚本自身，不影响服务。但图片序列是调试第二层的常用入口 |

**为什么 140 项测试全绿却没发现？**
`--source images` 这条路径没有任何测试覆盖，`测试结果.md` 也只验过视频链路。
README 第 52 行写了这条命令，但**文档里写了 ≠ 跑过**。

### 缺陷 2：真实服务下 WebSocket 完全不可用

| | |
| :--- | :--- |
| **现象** | 起真实 `uvicorn` 后 `/v1/stream` 返回 **404**，服务端日志警告 `No supported WebSocket library detected`；`scripts/ws_probe.py` 报「握手失败（服务起了吗？）」 |
| **根因** | README 要求 uvicorn **不许带 `[standard]`**（避免 httptools / uvloop 等 C 扩展），但没装任何替代 WS 库，`--ws auto` 无实现可选 |
| **修复** | `requirements.txt` 加 `wsproto>=1.2`。选它而**不是** `websockets`：`wsproto` 是 `py3-none-any` 纯 Python，`websockets` 是 `cpXXX-manylinux` C 扩展 wheel，Termux 上要编译，违反项目的零编译约束 |
| **影响** | 前端消费 WebSocket 播报这条链路整条不可用；第四层推送同样受阻 |

**为什么 140 项测试全绿却没发现？**
`tests/test_api.py` 的 WS 用例走 Starlette `TestClient`，是**进程内**调用，
根本不经过 uvicorn，所以永远测不到「uvicorn 缺 WS 实现」这件事。
这是**测试盲区**，不是偶发 —— 同类问题以后还会从这个口子漏过去。

**遗留：** 目前没有真起 uvicorn 的集成测试覆盖 `/v1/stream`，
该缺口未补。补法：起真实 uvicorn 后用 `ws_probe.py` 或 `httpx` 连一次 WS，
断言收到 `hello` 和广播的 `announcement`。

> **教训：** 上面两条都验证了同一件事 —— **`pytest` 全绿不等于真实服务能跑。**
> `TestClient` 走进程内，绕过了 uvicorn、绕过了网络、绕过了依赖装配。
> 交付前必须跑一遍 `bash scripts/smoke.sh` + 真起服务连一次 WebSocket。

"""语音识别（ASR / STT）实现 —— 目的地那一格的**服务端**识别通道。

★ 为什么需要它：端侧原来只用浏览器的 `SpeechRecognition`。那是**浏览器厂商
  的云服务**（Chrome / Edge → Google，Safari → Apple），key 烘在浏览器二进制
  里，页面里无处可配。后果是：

    · 国内网络基本不可用（`error === "network"`，实测）；
    · Chromium 裸构建、Electron 壳、部分国产浏览器根本没有可用服务；
    · 出了问题我们**什么都做不了** —— 既看不到失败率，也换不掉它。

  服务端识别把这一环拿回自己手里：同一把 dashscope key、同一条 `httpx` 调用、
  同一套「失败必须如实播报」的规矩。

★ 但它**不是**四层核心链路里的一环（目的地还能打字），所以：

    · 不进 `/v1/health` 的降级列表 —— 缺了它，「语音输入」这个可选入口会在
      按下的**那一刻**如实说清楚，而随手把它挂进 health 只会让「降级」这枚
      徽章长期亮着，把 VLM / 检测器 / 地图那三个真的降级淹掉；
    · `ASR=none`（默认）是**合法状态**，不是错误：它表示「没有接识别」，
      端侧会退回浏览器识别，两条都不行才写「语音不可用」并劝打字。

★ 失败必须**抛**，绝不返回空串：空串的含义是「听到了，但没听清」（用户该
  再说一次），而抛异常的含义是「这一拍根本没在听」（系统该说它哑了）。
  把这两件事混成一个返回值，用户就再也分不出「我没说清」和「系统没听见」。
"""

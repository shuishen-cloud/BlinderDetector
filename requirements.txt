# 灵眸伴途（简化版）—— 后端依赖
#
# ★ 全部为纯 Python wheel，零编译。
# ★ 严禁引入 fastapi / pydantic：它们依赖 pydantic-core (Rust)，
#   PyPI 只有 manylinux (glibc) wheel，在 Termux (bionic libc) 上装不了。
# ★ uvicorn 不要带 [standard] extra —— 会拉 httptools / uvloop 等 C 扩展。

starlette>=0.40
uvicorn>=0.30
httpx>=0.27
python-dotenv>=1.0

# ★ WebSocket 实现，不要删。
#   uvicorn 默认 --ws auto 要在 websockets / wsproto 里挑一个，
#   两个都没装时 /v1/stream 直接 404。wsproto 是纯 Python，Termux 可装。
wsproto>=1.2

# 测试
pytest>=8.0
pytest-timeout>=2.3   # 防止 WebSocket 用例等不到消息时永久挂死

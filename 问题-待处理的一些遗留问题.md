# 问题 —— 待处理的遗留问题

> 2026-09-20　｜　这份文档是**待处理清单**（谁在阻塞谁、怎么修）。
>
> **与 [文档-设计中的问题.md](文档-设计中的问题.md) 的分工：**
>
> | 文档 | 是什么 | 用法 |
> | :--- | :--- | :--- |
> | `文档-设计中的问题.md` | **自检审计记录** —— 每条判断「是不是真缺陷、算不算过度设计」 | 复盘时读 |
> | 本文档 | **待处理清单** —— 按阻塞关系排序，每条给修法和影响面 | 排期时读 |
>
> 两边的条目**可能指同一件事**，本文档只写「怎么处理」，不重复论证。

---

## 一、本轮新发现（尚未在别处登记）

来源：为「接真实外部服务」做接口调研时核实出来的。**每条都有实测证据。**

### 1.1 ★★ 真实 VLM 会让第一层的去重塌缩成一个 key

**证据**（实测 `scene.apply` + `vision_dedup_key`）：

```
前方有个红绿灯，现在是红灯  → scene_key=default → dedup=vision:scene:default
左侧一米左右有人经过        → scene_key=default → dedup=vision:scene:default
前方人行道畅通             → scene_key=default → dedup=vision:scene:default
```

**根因**：`openai_compat.py` 的 prompt 写的是「只输出那句话，不要解释」，
返回 `vision_detail(text, scene_conf=0.8)` —— `objects` / `ocr_results` **永远为空**。
而场景分类（`rules/scene.py`）靠这两个字段推导 `scene_key`。

**影响**：换真实 VLM 后第一层会**反复播同一句、场景变了也不播**（闸门看全是重复）。
等于把第一层废掉。**这是「接真实 VLM」的真实工作量所在**，不是填 `.env` 就行。

**怎么修**：改 prompt 要求 JSON（`scene_description` + `objects[]` + `ocr_results[]` + `scene_conf`），
写解析与容错。契约不用动 —— `vision_detail()` 的形状就是为它设计的。

**阻塞**：真实 VLM（`新功能与接口改动.md` §1.1）。

### 1.2 ★ `scene_conf` 硬编码 0.8 → 置信度对冲永不触发

`vision_detail(text, scene_conf=0.8)` 是写死的，真实 VLM 的置信度没被提取。
于是 `phrasing.hedge()`（低置信度时加对冲措辞）**永远不会触发** —— 一个已实现的
安全措辞机制在真实链路下是死的。随 1.1 一起修。

### 1.3 ★ VLM 网络错误会变成 500，且一条播报都不发

`describe_frame()` 里没有 `try/except`，`r.raise_for_status()` 一抛就冒到路由 →
客户端拿到 **500**。`PerceptionLayer.handle()` 只处理了「返回空」的情况，没处理「抛异常」。

**影响**：真实服务一定会超时/限流/欠费。届时用户听不到任何声音 ——
正是 design.md 点名的「最危险失效模式」。

**怎么修**：provider 层兜底（超时/401/429 → 返回空 + 标记降级），
并让路由把降级通知出去（见 1.4）。

### 1.4 ★★ `broadcast_degraded` 定义了但全项目无人调用

**证据**：

```
app/main.py:304   async def broadcast_degraded(reason: str) -> None:     ← 定义
app/main.py:366   app.state.broadcast_degraded = broadcast_degraded      ← 只赋值
（grep 全项目：没有任何调用点）
```

**影响**：design.md 反复强调的「**沉默不能有歧义**」在**运行时失效**这条路径上
并未实现。降级通告机制写好了，但没接线。而 `GET /v1/health` 是拉取式的 ——
前端不主动轮询就永远不知道系统哑了（调试台有轮询，真端不一定有）。

**怎么修**：路由捕获到层异常时调用 `broadcast_degraded`；或在 `publish` 里判断
「这一批有层失败」时自动推一条 `source=system` 的降级播报。

**阻塞**：所有真实外部服务（它们一定会失败）。

### 1.5 ★★ 「已通知您的家人」是空话

**证据**：

```
app/core/rules/sos.py:42      "family": "已通知您的家人",
app/core/rules/fall.py:268    "已通知您的家人，请保持冷静，不要移动。"
（grep rules/ 与 layers/：无 SMTP、无 webhook、无任何外发实现）
```

**影响**：**这不是「功能没做完」，是诚实性问题。** 在用户最危险的时刻告诉他
一件没发生的事 —— 他会因此停止自救、原地等一个不会来的人。

**怎么修**（两步）：
1. **立刻**：措辞改成不含断言的说法（「正在通知您的家人」），直到真有通道；
2. **接通道后**：把投递结果如实反映到文本（成功才说「已通知」），
   并给 `detail.emergency` 加 `delivery` 字段（只加可选字段，符合兼容性原则）。

**阻塞**：真实短信/推送通道；也是答辩时最容易被追问的一条。

**进展（2026-09-20）：第 1 步已完成。** `sos.py` 的 `_TARGET_TEXT`
（含「已同步通知社区网格员」）与 `fall.py` 的通知链文案全部改成「正在通知…」，
不再断言投递结果；`tests/test_fall.py`、`tests/test_sos.py` 各加一条
`test_wording_never_claims_delivery_happened` 回归用例，把「不得出现『已通知』」
变成红灯。第 2 步（`detail.delivery` + 按投递结果改写文案）等真实通道。

### 1.6 ★★ 真实检测库与项目的零编译约束冲突

**证据**（实测 wheel 标签）：

| 包 | wheel | Termux 可装? |
| :--- | :--- | :--- |
| `onnxruntime` | `cp313-cp313-manylinux_2_28_x86_64` | ❌ C 扩展 |
| `opencv-python-headless` | `cp37-abi3-manylinux_2_28_x86_64` | ❌ C 扩展 |
| `ultralytics` | `py3-none-any` | ⚠️ 纯 Python，但依赖 torch |

`requirements.txt` 顶部写着「所有包均为纯 Python wheel，Termux 上零编译安装」
—— **引入任何真实检测库都会打破它**。

**怎么修**：先做架构决策，二选一：
- **A 服务端跑检测** → Termux 不能再当服务器，依赖树变重；
- **B 端侧跑检测**（design.md D2 已预告）→ ★ **需要一个新入口**把端侧算出的
  障碍物送回来做分级措辞（如 `POST /v1/safety/grade`），否则安全策略会分叉到端上。

**阻塞**：真实检测模型。**这个决策不做，检测这块动不了。**

### 1.7 `read_image()` 在 layer 模块里，检测器要反向 import

**证据**：`app/core/layers/perception.py:32` 定义 `read_image()`。
真实检测器要读图，得 `from app.core.layers.perception import read_image` ——
**从 detector 反向依赖 layer**，层次是歪的。

**怎么修**：挪到 `app/contracts.py` 附近或单独的 `app/core/images.py`。

### 1.8 工程卫生：f-string / shell 转义已经咬了三次

同一类坑，本会话内出现三次，其中两次造成**静默错误**：

| 位置 | 症状 | 后果 |
| :--- | :--- | :--- |
| `export_contract.py` 的 `{"error": "..."}` | f-string 当成格式说明符 → `ValueError` | 脚本直接崩（幸好是崩） |
| 同上，`{"lat", "lng"}` | 当成表达式求值 → 生成文档里成了 Python 元组 `('lat', 'lng')` | **静默错误**：前端照抄必然对不上（已修） |
| 我写文档时用未加引号的 heredoc，`[standard]` 被 zsh 当通配符展开 | 文本变成「不带 ）」 | **静默错误**：文档出现语义空洞（已修） |

**怎么修**：写含 `{}` 的 f-string 用 `{{}}` 转义；写含 `[]`/`$` 的 heredoc 用
`<< 'PY'`（**带引号**）。另外建议给 `export_contract.py` 加个产物断言测试
（如「生成的文档里不应出现 `('`」），把这类静默错误变成红灯。

---

## 二、上轮已登记、仍未处理（交叉引用，不在此重复论证）

| 问题 | 在哪 | 一句话 |
| :--- | :--- | :--- |
| **播报仲裁能力成了空洞** | [设计中的问题 §零](文档-设计中的问题.md) | 打断/积压/最短播放时长已拆掉、端侧还没写。**若任务书要求交付，这是缺口** |
| 上传帧落在被静态托管的目录（隐私泄露） | 同上 §2.1 | 已实测 `/data/uploads/*` 可被 200 取回 |
| `sent`/`dropped` 无界增长 + 每响应全量序列化 | 同上 §2.2 | 实测 76 条 → 响应 2.3 KB，会涨到十万级 |
| 上传无大小上限 | 同上 §2.3 | 实测 3 MB 通过 |
| 一帧图像被搬运三遍 | 同上 §2.4 | 内存→磁盘→内存，只为保住 `image_ref` 语义 |
| 调试台静默停止发帧 | 同上 §2.5 | `video.paused` 直接 return，无任何提示 |
| 调试台不限制并发发送 | 同上 §2.6 | `setInterval` 不等上一次 fetch |
| `CORS_ORIGINS` 当前用不到（YAGNI） | 同上 §1.1 | 三行就能删 |

---

## 三、按阻塞关系排序的处理建议

**先做**（现在就在漏 / 会误导排查）：

1. ~~**1.5 「已通知」诚实化**~~ —— ✅ 已完成（2026-09-20，措辞 + 回归用例）
2. **2.1 上传目录隐私泄露**（交叉引用）—— 现在就在漏
3. **2.5 调试台静默停发**（交叉引用）—— 它已经让你误判过两轮
4. **1.4 接上 `broadcast_degraded`** —— 它所有真实服务的**前置条件**

**再做**（接真实服务之前必须完成）：

5. **1.1 + 1.2 + 1.3**（VLM 三件套：prompt → 去重塌缩、置信度、异常兜底）
6. **2.2 信封膨胀**（前端同步改 `a.sent`）

**需要先决策才能动**：

7. **1.6 检测模型：服务端还是端侧** —— 决策不做，这块动不了
8. **端侧仲裁**（交叉引用 §零）—— 且要先定交付载体（`工作管理.md` §六 #2）

**顺手**：

9. 1.7 `read_image` 挪位置、1.8 转义卫生、`CORS_ORIGINS` 删除

> **最该先问清的一件事**：任务书里「播报仲裁」算不算交付能力（`工作管理.md` §六 #4）。
> 若算，它的优先级高于本表所有条目。

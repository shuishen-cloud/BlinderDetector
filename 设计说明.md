# 设计说明

这份文档记录**为什么这么做**，不是**做了什么**。由完整版的
`docs/design.md` + 五篇管理文档压缩而来，只保留答辩时会被问到的那部分。

---

## 一、关键设计决策

### D1　全系统只有两个数据结构
`Frame`（输入）+ `Announcement`（输出），所有路由统一。四层由不同的人写，
接口越多越容易分叉；统一成一对信封后，新增一层不需要新增任何顶层类型。
**代价：** `detail` 是无类型 dict，丢了编译期检查，靠 `contracts.py` 里的
构造函数和 `test_contracts.py` 的形状断言补回来。

### D2　感知与安全是两条解耦的流水线 ★
第一层走云端 VLM（1–3 秒），第二层走轻量检测（<200 ms），内部完全独立，
只在播报出口汇合。

**理由：** 一个 3 秒后才播出的「前方 2 米有台阶」，用户早就走过去了。
所以安全预警在架构上就不能和场景描述共用一条链路。它决定了：第二层永远
不能调 VLM；两层的延迟预算、触发频率、去重策略全都不同；第二层将来下沉
到端侧模型时，第一层契约不用动。

**答辩要点：** 「为什么不能一个模型全干？」—— 延迟量级差 10 倍以上，
强行合并会让避障失效。

### D3　风险分级用悲观距离 ★
分级一律用 `distance - distance_sigma_m`，不用中位数估计。单目深度估计误差
30–50%，而且**误差是不对称地有害的**：

| 系统说 | 实际 | 后果 |
| :--- | :--- | :--- |
| 2 米 | 0.8 米 | 用户按 2 米的预期迈步 → **撞上** |
| 0.8 米 | 2 米 | 用户多绕一步 → 只是啰嗦 |

**配套：** 对障碍物不播距离数字，只播粗档（「就在脚前 / 很近 / 几米外」）。
「前方 2.3 米」这种虚假精度会让用户建立错误的心理预期。
**例外：** 第三层的距离来自地图/GPS，误差量级完全不同，可以播数字。

### D4　去重键必须含风险等级和距离档位
`dedup_key = f"obstacle:{type}:{position}:{risk}:{距离档位}"`。如果只用障碍物
类型，下面这个序列会被当成「同一个台阶的重复」全部吞掉：

```
台阶(info,   3.2m)  -> 播报
台阶(warning,1.8m)  -> 被去重丢弃
台阶(danger, 0.7m)  -> 被去重丢弃   ← 用户撞上去
```

被吞掉的恰恰是最危险的那条，因为它是「同一个物体的重复」。这是安全系统里
最经典的「告警抑制吞掉真实告警」缺陷 —— 违反直觉的地方在于，最危险的那条
看起来最像重复。

### D5　近距离预警用短句
播报文本按剩余时间窗自动降级：先试完整句，说不完就用最短形式。
用户以 1.1 m/s 走，2 米外的台阶只有 **2.7 秒**窗口，而「前方几米有向下的
台阶，注意脚下」需要 **3.7 秒**才播得完（中文 TTS 约 250 ms/字）——
**物理上就说不完。**

配套规则：`ttl_ms` 必须 ≥ 播报耗时，否则消息在播完之前就过期。

### D6　置信度门限：宁可少报，不可误报
障碍物置信度 < 0.5 直接不生成播报。播报的价值取决于用户是否相信它，
而**信任是消耗品**。误报率高到某个点，用户会开始忽略预警 —— 包括真的那一次
（告警疲劳，安全系统失效的头号原因）。推论：只报 3 类但每报必准的系统，
比报 12 类但一半是噪音的系统有价值得多。

### D7　跌倒 `suspected` 态永不自动外呼 ★
检测到冲击后先二次确认，用户不响应才升级到 `confirmed`，此时才通知家属。
真跌倒和「把手机扔到床上」「快速坐下」在加速度计上几乎无法区分；**用布尔值
表达这个状态意味着只有一个阈值旋钮，而这个旋钮往任何一边拧都是错的。**

两条配套规则：
1. **沉默 ≠ 安全。** 取消必须要求显式动作，绝不能因为「没检测到后续运动」
   就自动取消 —— 失去意识的人同样没有运动。
2. **取消不能依赖网络。** 端侧本地就能完成取消，服务端只是通知者；
   倒计时也是端本地自减，不靠服务端 tick 驱动。

**假阳性抑制：** 步行节律是硬否决；充电中 + 屏幕亮 + 长时间静止只延长二次
确认窗口，不构成取消理由。

### D8　绝不自动拨 120
升级链是「家属 → 社区网格员 → 建议手动拨号」。自动拨急救电话是法律责任最重
的一环，误报的代价是占用真实急救资源。

### D9　规则与编排分开
`detectors/` 只回答「看到什么」，`rules/` 回答「怎么判断危险、怎么说出来」，
`layers/` 只做编排。这样换检测模型时安全策略不用跟着变，规则层也能脱离
HTTP 单独测试 —— 138 项测试里绝大多数是这一层，安全关键逻辑（跌倒状态机、
风险分级）可以被穷举测试。

### D10　不用 FastAPI
开发环境是 Termux / Android，平台标签 `android_24_arm64_v8a`（bionic libc）。
`pydantic-core` 在 PyPI 上只有 `manylinux`（glibc）和 `musllinux` 的 wheel，
**没有一个能在 Android 上用**，pip 会退化成源码编译 Rust。
实测 `starlette` / `anyio` / `h11` / `idna` / `pytest` 全是纯 Python wheel。
**代价：** 失去自动 OpenAPI，用 `scripts/export_contract.py`（完整版里有）
从 dataclass 反向生成契约文档补回来。

### D11　播报出口：服务端只做闸门
四条流水线共用一个出口，但它**只做去重和废数据过滤**。排序、打断、积压保护、
到期不补播全部由端侧负责。

**理由：** 喇叭在端侧，服务端观察不到「当前正在播哪条、播到哪了」。初期版本
在服务端维护「当前播报 + 优先级队列」来模拟播放，连出两个**永久静默**的 bug，
根因完全相同 —— 服务端在猜自己看不见的东西。这不是实现失误，是这个设计
必然的产物。契约里 `ttl_ms` 的定义本来就写「**从端「收到」起算**」。

**代价：** 端侧要自己实现播放队列与优先级抢占，换载体就要重写一遍。

---

## 二、已知问题（完整版已登记，简化版同样存在）

1. **★「已通知您的家人」是空话。** `rules/sos.py` 与 `rules/fall.py` 都这么写，
   但全项目没有 SMTP / webhook / 任何外发实现。**这不是功能没做完，是诚实性
   问题**——在用户最危险的时刻告诉他一件没发生的事，他会因此停止自救。
   立刻的修法是措辞改成「正在通知您的家人」。
2. **★ 接真实 VLM 会让第一层去重塌缩成一个 key。** `openai_compat.py` 的
   prompt 只要一句话，返回的 `objects[]` / `ocr_results[]` 永远为空，
   而场景分类靠这两个字段推导 `scene_key`。于是场景变了也不播。
   **这是「接真实 VLM」的真实工作量所在，不是填 `.env` 就行。**
3. **`scene_conf` 硬编码 0.8**，低置信度对冲措辞（`phrasing.hedge()`）永不触发。
4. **VLM 网络错误会变成 500 且一条播报都不发**，provider 层缺 `try/except`。
5. **`broadcast_degraded` 定义了但没有调用点**，「沉默不能有歧义」在运行时
   失效这条路径上并未接线（`main.py` 里只赋给了 `app.state`）。
6. **真实检测库与零编译约束冲突。** `onnxruntime` / `opencv-python-headless`
   都是 C 扩展，Termux 装不了。**需要先决策：检测跑服务端还是端侧**——
   这个决策不做，检测这块动不了。

---

## 三、与完整版的差异

**本简化版裁掉了：**

| 裁掉的东西 | 为什么 |
| :--- | :--- |
| `web/index.html`（437 行调试台） | 不是交付形态，只为肉眼验证契约 |
| `/v1/frame` multipart 上传入口 | `image_ref` 走 JSON 已够跑通链路 |
| `/data` 静态托管 + CORS 中间件 | 为调试台服务，链路上不需要 |
| `scripts/`（抽帧、探针、契约导出） | 依赖 ffmpeg / librsvg，本地跑不动 |
| `assets/` `data/` 测试素材 | 生成的，可重建 |
| 五篇 Markdown 管理文档 | 压缩成本文档 |

**与完整版逐字节相同的文件（33 个）：** `contracts.py`、`arbiter.py`、
`registry.py`、`rules/*`、`layers/*`、`detectors/*`、`providers/*`、
`sources/{__init__,base}.py`、`mock/*`，以及除 `test_api.py` 外的全部测试。

**被改动的文件（只有这 5 个）：**

| 文件 | 改了什么 |
| :--- | :--- |
| `app/main.py` | 去调试台 / 静态托管 / 上传入口 / CORS，370 → 249 行 |
| `app/config.py` | 删 `CORS_ORIGINS`（中间件已移除，成了死配置） |
| `app/core/sources/video.py` | 文档字符串里的示例从已删脚本改成注册表用法 |
| `app/core/sources/images.py` | 同上 |
| `tests/test_api.py` | 删掉上传与调试台相关用例，336 → 158 行 |

测试数 156 → 138（减少的正好是被删掉的那 18 项）。

**怎么和完整版同步：** 契约或规则层有更新时，直接从完整版覆盖上面那些
「一行没改」的文件即可，`main.py` 需要手工合（主要是别把调试台相关的东西
带回来）。

**完整版在哪：** 你 fork 的 `BlinderDetector`，分支 `Lwy`
（默认分支 `main` 只有骨架，落后 10 个提交）。

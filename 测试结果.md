# 测试结果

> 执行日期：2026-09-20　｜　分支：`Lwy`　｜　提交：`245c6b3`
>
> **本报告对应上面这个提交。测试若再有改动，请重跑并更新本文档 —— 否则它就会像
> `55641f9` 那一版一样，列着一批已经不存在的用例。**

## 结论

**157 项测试全部通过，0 失败。** 端到端链路（起服务、打全部路由、喂视频、
跌倒全流程、契约文档生成）均正常。

| 项 | 结果 |
| :--- | :--- |
| 单元测试 / 集成测试 | **157 passed**, 0 failed, 0 error |
| 耗时 | 0.21 s |
| 端到端自检 `smoke.sh` | 通过 |
| 视频链路 `run_video.py data/demo.mp4` | 通过（8 帧，发出 13 条，丢弃 0 条） |
| 视频链路 `run_video.py data/frames --source images` | 通过（3 帧，发出 5 条，丢弃 0 条） |
| 跌倒演示 `demo_fall.py` | 通过（3 条路径，5 条播报） |
| 契约文档生成 | 通过（`docs/api-contract.md`，397 行） |
| 警告 | 1 条（Starlette TestClient 的 httpx 弃用提示，不影响结果） |

## 测试环境

| 项 | 版本 |
| :--- | :--- |
| 平台 | WSL2 / Linux x86_64（早期为 Termux / Android aarch64） |
| Python | 3.13.2 |
| starlette | 1.6.0 |
| uvicorn | 0.53.0 |
| httpx | 0.28.1 |
| pytest | 9.1.1 |
| pytest-timeout | 2.4.0 |
| python-dotenv | 1.2.3 |
| wsproto | 1.3.2 |
| python-multipart | 0.0.32 |

依赖全部为纯 Python wheel，**零编译**。

> 后两项（`wsproto` / `python-multipart`）是踩过坑之后补的：都不带 `[standard]`
> 的 uvicorn 没有 WS 实现、`request.form()` 没有 multipart 解析器，而**两者在
> 单元测试里都测不出来**（`TestClient` 走进程内，不经过 uvicorn）。详见
> [工作管理.md](工作管理.md) §七。

---

## 一、按文件统计

| 测试文件 | 用例数 | 覆盖对象 |
| :--- | ---: | :--- |
| `test_api.py` | 39 | 全部路由的形状一致性、`/v1/frame` 上传、WebSocket 广播、静态托管 |
| `test_risk.py` | 20 | 悲观距离分级、置信度门限、TTL 计算 |
| `test_contracts.py` | 18 | 两个信封的形状、距离档位、去重键 |
| `test_fall.py` | 17 | 跌倒状态机（覆盖最全） |
| `test_phrasing.py` | 16 | 不播数字、置信度对冲、短句降级 |
| `test_route.py` | 14 | 无障碍过滤、导航播报 |
| `test_scene.py` | 14 | 场景分类与去重粒度 |
| `test_sos.py` | 11 | 幂等、升级链、绝不自动拨 120 |
| `test_arbiter.py` | 8 | 播报闸门：去重窗口、升级突破去重、废数据 |
| **合计** | **157** | |

其中 `test_risk.py` / `test_phrasing.py` / `test_scene.py` / `test_fall.py` /
`test_sos.py` / `test_route.py` 共 92 项只测 `app/core/rules/`，**不依赖
Starlette，可脱离框架运行** —— 这是把业务规则从编排层抽出来的直接收益。

---

## 二、详细用例清单

### `tests/test_api.py` —— 接口层（39 项，全部通过）

**五条业务路由的形状一致性**（参数化，10 项）：

- `test_route_accepts_frame_returns_announcement[5 条路由各一次]` — PASSED ×5
- `test_every_route_outputs_same_envelope[5 条路由各一次]` — PASSED ×5

**基础行为**（3 项）：

- `test_frame_id_is_echoed_back` — PASSED
- `test_empty_body_still_works` — PASSED
- `test_bad_json_does_not_500` — PASSED

**健康检查**（2 项）：

- `test_health_reports_impls` — PASSED
- `test_health_lists_framesource_impls` — PASSED

**闸门接进了路由**（3 项）：

- `test_safety_alert_reaches_arbiter` — PASSED
- `test_clear_path_produces_no_announcement` — PASSED
- `test_repeated_perception_frame_is_deduped` — PASSED

**WebSocket**（2 项）：

- `test_ws_receives_broadcast` — PASSED
- `test_ws_ping_pong` — PASSED

**`POST /v1/frame` 统一帧入口**（17 项）：

- `test_upload_returns_announcement[perception | safety]` — PASSED ×2
- `test_upload_outputs_same_envelope[perception | safety]` — PASSED ×2
- `test_upload_defaults_to_perception` — PASSED
- `test_upload_extra_reaches_the_layer` — PASSED
- `test_upload_frame_id_and_ts_are_echoed` — PASSED
- `test_upload_broadcasts_to_ws` — PASSED
- `test_upload_reaches_the_arbiter` — PASSED
- `test_upload_without_image_is_rejected` — PASSED
- `test_upload_with_image_as_plain_field_is_rejected` — PASSED
- `test_upload_rejects_unknown_source` — PASSED
- `test_upload_rejects_malformed_extra` — PASSED
- `test_upload_rejects_non_object_extra` — PASSED
- `test_upload_rejects_empty_image` — PASSED
- `test_upload_leaves_no_temp_file` — PASSED
- `test_far_future_tick_does_not_wedge_the_gate` — PASSED

**前端调试台与素材托管**（2 项）：

- `test_homepage_serves_the_console` — PASSED
- `test_data_mount_serves_test_assets` — PASSED（素材不存在时按条件 SKIP，
  不让 fresh clone 因为这个用例挂掉）

### `tests/test_risk.py` —— 风险分级（20 项，全部通过）

- `test_pessimistic_distance_subtracts_sigma` — PASSED
- `test_pessimistic_distance_never_negative` — PASSED
- `test_pessimistic_distance_handles_missing_sigma` — PASSED
- `test_sigma_changes_the_bucket` — PASSED
- `test_low_confidence_is_not_published` — PASSED
- `test_confidence_at_threshold_passes` — PASSED
- `test_obstacle_at_feet_is_danger` — PASSED
- `test_fast_approaching_object_is_danger` — PASSED
- `test_slow_approaching_object_is_not_danger` — PASSED
- `test_static_object_has_no_ttc` — PASSED
- `test_nearby_step_is_at_least_warning` — PASSED
- `test_person_is_low_severity` — PASSED
- `test_severe_types_have_a_wider_alert_horizon` — PASSED
- `test_step_degrades_to_info_before_entering_the_warning_band` — PASSED
- `test_unknown_type_falls_back_to_default_severity` — PASSED
- `test_glass_door_is_treated_as_severe` — PASSED
- `test_ttl_scales_with_distance` — PASSED
- `test_ttl_is_longer_for_slower_walkers` — PASSED
- `test_ttl_is_clamped` — PASSED
- `test_ttl_handles_zero_speed` — PASSED

### `tests/test_contracts.py` —— 契约（18 项，全部通过）

- `test_frame_constructs_and_serializes` — PASSED
- `test_announcement_required_fields` — PASSED
- `test_announcement_to_json_is_utf8_not_escaped` — PASSED
- `test_all_sources_have_a_fixture` — PASSED
- `test_every_fixture_detail_has_kind` — PASSED
- `test_vision_detail_shape` — PASSED
- `test_positions_are_legal` — PASSED
- `test_bbox_is_normalized` — PASSED
- `test_safety_detail_shape` — PASSED
- `test_route_detail_shape` — PASSED
- `test_emergency_detail_shape` — PASSED
- `test_system_detail_shape` — PASSED
- `test_distance_bucket_boundaries` — PASSED
- `test_dedup_key_changes_when_risk_escalates` — PASSED
- `test_dedup_key_changes_when_distance_bucket_changes` — PASSED
- `test_fixture_safety_dedup_key_matches_its_obstacle` — PASSED
- `test_critical_priority_gets_strong_haptic` — PASSED
- `test_background_priority_is_silent` — PASSED

### `tests/test_fall.py` —— 跌倒状态机（17 项，全部通过）

- `test_real_fall_enters_suspected_and_asks` — PASSED
- `test_weak_impact_is_ignored` — PASSED
- `test_no_free_fall_is_ignored` — PASSED
- `test_walking_cadence_is_a_hard_negative` — PASSED
- `test_same_event_is_idempotent` — PASSED
- `test_no_notification_before_deadline` — PASSED
- `test_notifies_only_after_deadline` — PASSED
- `test_silence_never_counts_as_safe` — PASSED
- `test_phone_dropped_on_ground_still_escalates` — PASSED
- `test_benign_context_only_extends_the_window` — PASSED
- `test_explicit_cancel_stops_escalation` — PASSED
- `test_cancel_reports_method_and_elapsed` — PASSED
- `test_cancel_unknown_event_is_noop` — PASSED
- `test_cancel_twice_is_noop` — PASSED
- `test_notification_chain_order_and_dedup_keys` — PASSED
- `test_never_auto_dials_emergency_services` — PASSED
- `test_cancel_does_not_need_network` — PASSED

### `tests/test_phrasing.py` —— 措辞（16 项，全部通过）

- `test_speech_estimate_is_chars_times_rate` — PASSED
- `test_ensure_ttl_extends_when_too_short` — PASSED
- `test_ensure_ttl_passes_when_long_enough` — PASSED
- `test_obstacle_phrase_never_contains_a_number` — PASSED
- `test_distance_words_cover_all_buckets` — PASSED
- `test_low_confidence_gets_hedged` — PASSED
- `test_high_confidence_is_not_hedged` — PASSED
- `test_obstacle_phrase_hedges_low_confidence` — PASSED
- `test_terse_is_shorter_than_full` — PASSED
- `test_fit_text_full_when_window_is_roomy` — PASSED
- `test_fit_text_falls_back_to_terse_when_window_is_tight` — PASSED
- `test_fit_text_still_returns_something_when_impossible` — PASSED
- `test_most_urgent_obstacle_comes_first` — PASSED
- `test_safety_text_caps_the_number_of_items` — PASSED
- `test_empty_input_produces_empty_text` — PASSED
- `test_danger_gets_stronger_haptic_than_warning` — PASSED

### `tests/test_route.py` —— 导航（14 项，全部通过）

- `test_plan_returns_well_formed_detail` — PASSED
- `test_overpass_step_is_filtered_out` — PASSED
- `test_filtering_produces_a_warning` — PASSED
- `test_no_avoid_keeps_all_steps` — PASSED
- `test_total_distance_excludes_filtered_steps` — PASSED
- `test_route_id_is_stable_for_same_destination` — PASSED
- `test_route_id_differs_for_different_destinations` — PASSED
- `test_step_announcement_may_contain_numbers` — PASSED
- `test_step_announcement_uses_navigation_source` — PASSED
- `test_only_one_step_is_spoken_by_default` — PASSED
- `test_max_steps_is_respected` — PASSED
- `test_step_dedup_keys_are_distinct` — PASSED
- `test_ttl_is_long_enough_for_the_text` — PASSED
- `test_warnings_are_announced` — PASSED

### `tests/test_scene.py` —— 场景分类（14 项，全部通过）

- `test_empty_scene_is_clear` — PASSED
- `test_traffic_light_wins_over_other_objects` — PASSED
- `test_person_is_split_by_side` — PASSED
- `test_english_labels_work` — PASSED
- `test_bicycle_is_not_mistaken_for_a_car` — PASSED
- `test_result_is_independent_of_object_order` — PASSED
- `test_steps_are_recognized` — PASSED
- `test_ocr_category_used_when_no_objects` — PASSED
- `test_objects_take_precedence_over_ocr` — PASSED
- `test_unknown_objects_fall_back_to_clear` — PASSED
- `test_classification_is_coarse_enough_to_dedupe` — PASSED
- `test_scene_key_changes_when_scene_actually_changes` — PASSED
- `test_apply_fills_missing_scene_key` — PASSED
- `test_apply_keeps_provided_scene_key` — PASSED

### `tests/test_sos.py` —— 求助状态机（11 项，全部通过）

- `test_trigger_notifies_family_first` — PASSED
- `test_idempotency_key_prevents_double_call` — PASSED
- `test_different_keys_are_independent` — PASSED
- `test_no_escalation_before_timeout` — PASSED
- `test_escalates_to_grid_worker_after_timeout` — PASSED
- `test_never_auto_dials_emergency_services` — PASSED
- `test_chain_stops_at_the_end` — PASSED
- `test_acknowledge_stops_escalation` — PASSED
- `test_cancel_resolves_the_event` — PASSED
- `test_acknowledge_unknown_event_is_noop` — PASSED
- `test_notification_keys_are_distinct` — PASSED

### `tests/test_arbiter.py` —— 播报闸门（8 项，全部通过）

> **★ 用例集换过一批。** 闸门现在只做两件事：去重、丢废数据。
> 「打断 / 积压保护 / 最短播放时长」已移到端侧（见
> [docs/design.md](docs/design.md) D11），所以那批用例随之删除 ——
> 它们测的是一个已不存在的实现，留着只会全红。
> **但请记住：那三个能力现在既无实现也无测试**，见 §五 已知限制。

- `test_new_key_is_sent` — PASSED
- `test_priority_does_not_gate_anything` — PASSED（优先级不参与闸门判断）
- `test_duplicate_within_window_is_dropped` — PASSED
- `test_same_key_after_window_is_sent_again` — PASSED
- `test_escalation_breaks_dedup` — PASSED　★ 最要紧的一条
- `test_dedup_survives_a_backwards_clock` — PASSED
- `test_zero_ttl_is_dropped_immediately` — PASSED
- `test_drop_reason_lookup` — PASSED

---

## 三、安全关键规则的验证情况

以下几项测试验证的是**会实际致害的失效模式**，答辩时可单独指出：

| 验证的规则 | 对应用例 | 若失效的后果 |
| :--- | :--- | :--- |
| 风险升级不会被去重吞掉 | `test_escalation_breaks_dedup`、`test_dedup_key_changes_when_risk_escalates` | 台阶从 info 升到 danger 时被当成「重复」丢弃，用户撞上去 |
| 风险分级用悲观距离 | `test_pessimistic_distance_subtracts_sigma`、`test_sigma_changes_the_bucket` | 高估距离导致用户按错误预期迈步 |
| 对障碍物不播距离数字 | `test_obstacle_phrase_never_contains_a_number` | 虚假精确感让用户建立错误心理预期 |
| TTL 不小于播报耗时 | `test_ensure_ttl_extends_when_too_short` | 预警播到一半过期，用户两句话都没听懂 |
| 近距离自动降级为短句 | `test_fit_text_falls_back_to_terse_when_window_is_tight` | 完整句子物理上说不完，等于没报 |
| 置信度门限 | `test_low_confidence_is_not_published` | 误报累积导致告警疲劳，真预警被忽略 |
| 跌倒 `suspected` 永不提前外呼 | `test_no_notification_before_deadline`、`test_notifies_only_after_deadline` | 把手机扔到床上就打电话给家属 |
| 沉默不等于安全 | `test_silence_never_counts_as_safe`、`test_phone_dropped_on_ground_still_escalates` | 失去意识的人没有运动，被误判为「没事」 |
| 取消不依赖网络 | `test_cancel_does_not_need_network` | 断网时用户无法取消误报 |
| 绝不自动拨 120 | `test_never_auto_dials_emergency_services`（fall 与 sos 各一条） | 误报占用真实急救资源，法律责任最重 |
| 幂等防止重复呼叫 | `test_idempotency_key_prevents_double_call` | 网络重试变成三个电话打给家属 |
| 通知链去重键互不相同 | `test_notification_keys_are_distinct` | 网格员永远收不到通知 |

---

## 四、端到端验证

### `scripts/smoke.sh` —— 起服务打全部路由

```
==> 全部通过
```

十条路由逐一验证，含健康检查、统一帧入口与紧急状态机时钟推进。

### `scripts/run_video.py data/demo.mp4` —— 视频链路

```
输入源 : video  (8 帧)
参与层 : perception, safety
共发出 13 条，丢弃 0 条
```

验证了：

- 场景轮换（红绿灯 / 行人 / 畅通）正常逐帧产出
  （**丢弃 0 条**：素材 1 fps、3 个场景轮换，同一场景间隔正好 3 秒，
  不小于 3 秒去重窗口，所以碰不到去重。）
- 安全预警自动降级为短句（「台阶，注意脚下」而非完整句）——
  2 米外的台阶只有 2.7 秒窗口，完整句需要 3.7 秒
- 置信度 0.41 的误检被门限滤除，未进入播报

### `scripts/demo_fall.py` —— 跌倒全流程

```
路径 A：真跌倒，用户没响应  -> suspected -> (15s 无响应) -> confirmed -> 通知家属 -> 升级网格员
路径 B：真跌倒，用户摇一摇取消 -> suspected -> cancelled -> 窗口归零后无外呼
对照：步行节律              -> 直接判为误报，连询问都不发
```

### `scripts/export_contract.py` —— 契约文档生成

```
已生成 docs/api-contract.md  (397 行)
```

---

## 五、已知限制

| 项 | 说明 |
| :--- | :--- |
| 外部服务全部为 mock | 不调云端 VLM、不跑检测模型、不连地图 API、不发真实短信 |
| 未在端侧验证 | 加速度计、摄像头、震动、物理按键均未接入真实设备 |
| 未做性能测试 | 无延迟、吞吐、内存占用的基准数据 |
| WebSocket 测试覆盖有限 | 仅验证广播与 ping/pong，未覆盖断线重连与重放 |
| **★ 播报仲裁能力：无实现、无测试** | 打断 / 积压保护 / 最短播放时长已随服务端状态机一起拆掉，改由端侧负责，而端侧还没写。**若任务书把它列为交付能力，这是个缺口**（见 工作管理.md §六 #4） |
| 上传帧的临时目录被静态托管暴露 | `UPLOAD_DIR` 在 `/data` 挂载点之下，公开可取（已实测）。见 [文档-设计中的问题.md](文档-设计中的问题.md) §2.1 |
| 闸门的观测列表无界增长 | `sent` / `dropped` 只增不减，且每个响应全量序列化 `sent_ids`。见 §2.2 |
| 1 条弃用警告 | Starlette TestClient 建议改用 httpx2，不影响功能 |
| `run_video.py` 不再演示去重 | 素材 1 fps × 3 场景轮换刚好卡在 3 秒窗口边界；要看去重得把帧率调密 |

---

## 六、如何重现

```bash
cd /home/lwy/project/BlinderDetector
pip install -r requirements.txt

python -m pytest tests/ -v          # 157 项
bash scripts/smoke.sh               # 端到端自检
python scripts/run_video.py data/demo.mp4
python scripts/demo_fall.py
```
